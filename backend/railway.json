{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm install && npm run build"
  },
  "deploy": {
    "startCommand": "PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium npm run migrate && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

